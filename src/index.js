import React from "react";
import ReactDOM from "react-dom/client";
import CountryAndStateDropdowns from './CountryAndStateDropdowns';

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <CountryAndStateDropdowns />
  </React.StrictMode>
);
